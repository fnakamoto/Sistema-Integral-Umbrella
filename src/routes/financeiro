const express = require('express');
const router = express.Router();
const financeiroController = require('../controllers/financeiroController');

router.post('/cobranca', financeiroController.gerarCobranca);

module.exports = router;
